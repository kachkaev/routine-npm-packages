---
"@kachkaev/eslint-config-base": patch
"@kachkaev/eslint-config-next": patch
---

Simplify README setup examples (no more spreading into `defineConfig`) and fix mistakes in the Next.js README (wrong package name in the install command, wrong function name in the `tsconfigRootDir` and `tailwindcssEntryPoint` snippets)
