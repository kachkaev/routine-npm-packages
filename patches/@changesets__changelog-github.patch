diff --git a/dist/changesets-changelog-github.cjs.js b/dist/changesets-changelog-github.cjs.js
index f0ab3c46b9cd516ca5635e2e4dab079bac2908a6..a2e6bd0c86d5d0d3f7918db28d9a82dbc14441d7 100644
--- a/dist/changesets-changelog-github.cjs.js
+++ b/dist/changesets-changelog-github.cjs.js
@@ -133,7 +133,8 @@ const changelogFunctions = {
         user: null
       };
     })();
-    const users = usersFromSummary.length ? usersFromSummary.map(userFromSummary => `[@${userFromSummary}](https://github.com/${userFromSummary})`).join(", ") : links.user;
+    // Related to https://github.com/changesets/changesets/pull/1255
+    const users = usersFromSummary.length ? usersFromSummary.filter(userFromSummary => userFromSummary !== "kachkaev").map(userFromSummary => `[@${userFromSummary}](https://github.com/${userFromSummary})`).join(", ") : links.user;
     const prefix = [links.pull === null ? "" : ` ${links.pull}`, links.commit === null ? "" : ` ${links.commit}`, users === null ? "" : ` Thanks ${users}!`].join("");
     return `\n\n-${prefix ? `${prefix} -` : ""} ${firstLine}\n${futureLines.map(l => `  ${l}`).join("\n")}`;
   }
diff --git a/dist/changesets-changelog-github.esm.js b/dist/changesets-changelog-github.esm.js
index cc3b130e10445e0ff561568c9ace6a1c94bdf960..2978b8eb50fc182efcb5b65af91a12b3d94397d7 100644
--- a/dist/changesets-changelog-github.esm.js
+++ b/dist/changesets-changelog-github.esm.js
@@ -129,7 +129,8 @@ const changelogFunctions = {
         user: null
       };
     })();
-    const users = usersFromSummary.length ? usersFromSummary.map(userFromSummary => `[@${userFromSummary}](https://github.com/${userFromSummary})`).join(", ") : links.user;
+    // Related to https://github.com/changesets/changesets/pull/1255
+    const users = usersFromSummary.length ? usersFromSummary.filter(userFromSummary => userFromSummary !== "kachkaev").map(userFromSummary => `[@${userFromSummary}](https://github.com/${userFromSummary})`).join(", ") : links.user;
     const prefix = [links.pull === null ? "" : ` ${links.pull}`, links.commit === null ? "" : ` ${links.commit}`, users === null ? "" : ` Thanks ${users}!`].join("");
     return `\n\n-${prefix ? `${prefix} -` : ""} ${firstLine}\n${futureLines.map(l => `  ${l}`).join("\n")}`;
   }
